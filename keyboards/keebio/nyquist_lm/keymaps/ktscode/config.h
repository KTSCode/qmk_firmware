#pragma once

// Default rgb_matrix color: green (HSV hue, 0-255 scale: 0 red, 85 green, 170 blue).
#define RGB_MATRIX_DEFAULT_HUE 85

// --- TEMPORARY (remove with the live tapping-term tuner) ---
// 10ms steps for the DT_DOWN/DT_UP tapping-term keys.
#define DYNAMIC_TAPPING_TERM_INCREMENT 10
